- hosts: localhost
  connection: localhost
  become: true

  pre_task:
    - name: update repositories
      pacman:
        update_cache: yes
      changed_when: yes

  tasks:
    - include: tasks/user.yml
    - include: tasks/packages.yml
